---
spring:
  config:
    activate:
      on-profile: default
  sql:
    init:
      schema-locations: classpath:schema-h2.sql
  data-source:
    auto-config:
      enabled: false
cds:
  security:
    mock.users:
      admin:
        password: admin
        roles:
          - admin
          - system-user
      user:
        password: user